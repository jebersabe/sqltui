[project]
name = "sqltui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "pandas>=2.3.3",
    "pyodps>=0.12.5",
    "python-dotenv>=1.2.1",
    "pyyaml>=6.0.3",
    "textual[syntax]>=6.6.0",
]

[dependency-groups]
dev = [
    "black>=25.11.0",
    "marimo>=0.18.0",
    "textual-dev>=1.8.0",
    "pyzmq>=27.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
sqltui = "sqltui.__main__:main"
